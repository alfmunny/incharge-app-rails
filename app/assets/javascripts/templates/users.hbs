<div class="container-fluid">
  <div class="row">
    <div class="col-md-3">
      <table class="table">
       <thead>
         <tr>
           <th>
             Users
             {{#link-to 'users.new' class="btn btn-primary btn-sm" }}Add User{{/link-to}} 
           </th>
         </tr>
       </thead>
      <tbody>
        {{#each}}
          <tr>
            <td>
            {{#link-to 'user' this}}
              {{name}}
            {{/link-to}}
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    </div>
    <div class="col-md-6">
      {{outlet}}
    </div>
  </div>
</div>
