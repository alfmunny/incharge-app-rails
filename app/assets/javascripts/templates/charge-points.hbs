<div class="container-fluid">
  <div class="row">
    <div class="col-md-3">
      <table class="table">
       <thead>
         <tr>
           <th>
             Charge Point
             {{#link-to 'charge-points.new' class="btn btn-primary btn-sm" }}Add Charge Point{{/link-to}} 
           </th>
         </tr>
       </thead>
      <tbody>
        {{#each controller}}
          <tr>
            <td>
            {{#link-to 'charge-point' this}}
              {{name}}
            {{/link-to}}
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
    </div>
    <div class="col-md-6">
      {{outlet}}
    </div>
  </div>
</div>
